rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // للـ Demo: نسمح بالقراءة للجميع، والكتابة لأي مستخدم مسجل دخول
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
